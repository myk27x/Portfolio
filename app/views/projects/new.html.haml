= link_to "home", root_path

#projectForm
  = form_for Project.new do |f|
    = f.label :title, "Title"
    = f.text_field :title
    = f.label :image, "Image"
    = f.text_field :image

    = f.submit "SUBMIT"

= @projects.count
